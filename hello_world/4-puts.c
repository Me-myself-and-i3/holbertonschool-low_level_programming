#include <stdio.h>

/**
 * @file main.c
 * @brief A simple C program using puts to print a specific string.
 *
 * This program demonstrates the usage of the puts function to print
 * the string "Programming is like building a multilingual puzzle"
 * followed by a newline character ('\n'). It adheres to the constraints
 * of not using printf and ending the program with a return value of 0.
 *
 * @details
 * The program includes the standard input-output library <stdio.h>.
 * It defines the main function, which:
 * - Calls the puts function with the string "Programming is like building
 *   a multilingual puzzle".
 * - Returns 0 to indicate successful completion of the program.
 *
 * @note
 * The program does not utilize printf as per the requirements.
 * It terminates explicitly with 'return 0;', adhering to the C standard.
 */
#include <stdio.h>

/**
 * @brief The main function of the program.
 *
 * @return 0 indicating successful execution.
 */
int main(void)
{
puts("Programming is like building a multilingual puzzle");
return (0);
}
